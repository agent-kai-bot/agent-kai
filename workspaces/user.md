User prefers autonomous execution: when strategy research is underway, keep pushing the analyst through next rounds without asking follow-up permission; proactively iterate until a clear conclusion is reached.
§
User wants autonomous workflow: keep pushing the analyst without asking follow-up for iterative backtesting/research status updates.
§
User wants autonomous workflow: proactively keep pushing the analyst on backtesting/research without asking follow-up permission each iteration.
§
User may invoke 'auto strategy research' to mean: start autonomous iterative research immediately, keep pushing without follow-up permission, and report only meaningful updates.
§
User prefers that any package installs or Python environment setup use a virtualenv; do not install packages on the host system.
§
User prefers that any package installs or Python environment setup use a virtualenv; do not install packages on the host.
§
User prefers automated multi-iteration strategy research in auto mode, especially for large symbol sweeps and backtesting. Do not install Python packages on the host; use virtualenv (or sandbox) for any environment setup.