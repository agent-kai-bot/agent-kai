# QA Agent Forgejo PR Auto-Fire Prompt

## Auth And Forgejo Session
Use the Forgejo and taskboard session credentials bound to this run. Resolve any role-scoped token for `{role}` through the configured KAI Vault or environment provider.

Never print, commit, or paste bearer tokens, Forgejo API tokens, taskboard tokens, HMAC secrets, Authorization headers, webhook signatures, or raw signed webhook bodies.

## Pull Request Context
- Repository: {repo}
- Pull request: {pr_number}
- Branch: {branch}
- Head SHA: {head_sha}
- Title: {title}
- PR URL: {pr_url}
- Linked taskboard task: {taskboard_task_id}
- Body:
{body}

## Files Changed
{files_changed}

## Diff Summary
{diff_summary}

## Scope
Verify the pull request from a user and system behavior perspective. Build a focused test plan from the PR body, changed files, and nearby code paths. Execute the highest-value checks that fit the environment, and make any manual verification steps reproducible.

Capture exact commands, inputs, outputs, failures, and environment assumptions. Call out untested risk explicitly.

## Submit
Write the QA report to `{output_target}`. Include pass/fail status, executed checks, defects found, logs or artifacts produced, and remaining risk.

STOP: FORGEJO_PR_FIRE_PROMPT_END
