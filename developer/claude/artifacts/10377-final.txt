Task 10377 developer fix-loop summary

Addressed code-reviewer/claude/artifacts/10377-final.txt Decision: CHANGES_REQUESTED:
1. Fixed daemon/auto_cost.py cumulative evaluator spend alerting. The alert predicate and reported spend_ratio now use total session auto-evaluator cost divided by session main-agent spend, rather than only the current critic call.
2. Fixed agent/auto_loop_brain.py prompt truncation. Critic prompt construction now deterministically preserves the first history message/task anchor plus latest turns and the structured AutoEvaluationInput/current main_response instead of keeping only a rightmost JSON suffix.

Regression coverage added/updated:
- tests/test_auto_cost.py verifies two individually sub-threshold critic calls alert when cumulative evaluator spend exceeds 5% of main-agent spend.
- tests/test_auto_loop_brain.py verifies over-budget history still includes the original task anchor, latest assistant turn, and current main response.
- tests/test_auto_mode.py fixture expectation updated to cumulative alert semantics.

Tests run:
- pytest -q tests/test_auto_loop_brain.py tests/test_auto_cost.py -q — passed (14 tests).
- pytest -q tests/test_auto_loop_brain.py tests/test_auto_cost.py tests/test_auto_mode.py -q — blocked during collection: ModuleNotFoundError: No module named 'langchain'.

Known follow-up/risk:
- Full auto-mode integration test should be re-run in CI/runtime with langchain installed.
- Forgejo PR creation could not be completed from this environment because FORGEJO_URL is not configured.
- Taskboard move/comment APIs returned 409/400 because taskboard reports the task as Backlog/not active despite the bound session token.

Commit: 124251c
Branch: task-10377-feat-implement-auto-loop-brain-llm-critic-per-10375-spec
