Task 10388 developer fix-loop summary

Prior verdict addressed:
- code-reviewer/claude/artifacts/10388-final.txt: Decision CHANGES_REQUESTED found OpenAI-compatible routing sent the auto-loop-brain default model_id (`sonnet`) to endpoints like `kai-local`, instead of using the endpoint-declared model (`qwen35-gptq`).

Implementation:
- Updated OpenAICompatToollessLLMClient to resolve the endpoint model when the incoming model is the Claude-oriented default (`sonnet`) or empty.
- Preserved explicit operator model overrides: non-default model_id values are still sent unchanged.
- Added regressions proving `kai-local`-style endpoint config sends `qwen35-gptq`, not `sonnet`, while preserving the tool-less request payload guarantee.
- Added factory coverage proving OpenAI client creation uses the endpoint-declared model for default config.

Commit:
- acf81de3a792116d6325e25b46ed6dd275b804f3 fix(auto-loop-brain): use endpoint model for openai default

Tests run:
- pytest -q tests/test_auto_loop_brain.py — 22 passed, 5 subtests passed.

Known follow-up / risk:
- No live LLM provider calls were made.
- Broader suites were not re-run in this fix-loop; prior review noted optional dependency gaps in this environment for some broader collections.
